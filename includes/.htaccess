# Dossier privé - aucun accès depuis le web
# Les fichiers d'includes ne doivent jamais être ouverts directement.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
