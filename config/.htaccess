# Dossier privé - aucun accès depuis le web
# Contient les paramètres BDD - protection critique.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
