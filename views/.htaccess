# Dossier privé - aucun accès depuis le web
# Les vues sont incluses par les contrôleurs dans /public/, jamais ouvertes directement.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
