# Dossier privé - aucun accès depuis le web
# Les classes sont chargées par les scripts dans /public/, jamais directement.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
